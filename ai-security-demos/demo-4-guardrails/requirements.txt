openai>=1.40.0
python-dotenv>=1.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
httpx>=0.27.0
# Optional framework wrapper — often fails to build on Windows (sentencepiece).
# Heuristic + Llama Guard layers still run without it (see guards.py fallback).
# llm-guard>=0.3.0
